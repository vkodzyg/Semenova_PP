<Window x:Class="Semenova_ind.SalesHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Semenova_ind"
        mc:Ignorable="d"
        Title="Семенова - История продаж" 
        Height="450" Width="700"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="HeaderStyle">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="10"/>
        </Style>

        <Style TargetType="ListViewItem">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#FFE0E0E0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleTextBlock" 
                   Text="История продаж"
                   Style="{StaticResource HeaderStyle}"
                   Grid.Row="0"/>

        <Border Grid.Row="1" 
                Background="#FFF5F5F5" 
                Padding="10" 
                Margin="0,0,0,10"
                CornerRadius="5">
            <StackPanel>
                <TextBlock x:Name="PartnerNameTextBlock" 
                           Text="Партнер: "
                           FontSize="16"
                           FontWeight="SemiBold"/>
                <TextBlock x:Name="DiscountTextBlock" 
                           Text="Текущая скидка: 0%"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <ListView x:Name="SalesListView" 
                  Grid.Row="2"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">

            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Дата" Width="120">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding SaleDate, StringFormat=dd.MM.yyyy}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Наименование продукции" Width="250">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Product.Name}" 
                                           FontWeight="Bold"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Количество" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Quantity}" 
                                           TextAlignment="Center"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Цена" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Product.Price, StringFormat={}{0:N2} ₽}" 
                                           TextAlignment="Right"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Сумма" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TotalAmount, StringFormat={}{0:N2} ₽}" 
                                           TextAlignment="Right"
                                           FontWeight="Bold"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <Border Grid.Row="3" 
                Background="#FFF0F0F0" 
                Padding="10" 
                Margin="0,10,0,0"
                CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="TotalSalesTextBlock" 
                           Grid.Column="0"
                           Text="Всего продаж: 0"
                           VerticalAlignment="Center"/>

                <Button x:Name="CloseButton" 
                        Grid.Column="1"
                        Content="Закрыть"
                        Width="100"
                        Height="30"
                        Click="CloseButton_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>